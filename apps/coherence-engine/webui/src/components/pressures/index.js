/**
 * PressuresEditor module
 *
 * Main entry point - only exports the main component.
 * Internal modules (cards, modals, shared) are used internally.
 */

export { default, PressuresEditor } from './PressuresEditor';
