/**
 * DependencyViewer module
 *
 * Main entry point - only exports the main component.
 * Internal modules (components) are used internally.
 */

export { default, DependencyViewer } from './DependencyViewer';
