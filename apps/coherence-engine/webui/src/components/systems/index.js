/**
 * SystemsEditor module
 *
 * Main entry point - only exports the main component.
 * Internal modules (cards, tabs, shared) are used internally.
 */

export { default, SystemsEditor } from './SystemsEditor';
