{
  "name": "@name-forge/webui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5001",
    "dev:standalone": "STANDALONE=true vite --port 3000",
    "clean": "rm -rf dist",
    "build": "npm run clean && vite build",
    "lint": "eslint .",
    "preview": "vite preview",
    "static": "pnpm run build && pnpm exec serve dist -p 3000",
    "serve": "pnpm exec serve dist -p 3000"
  },
  "dependencies": {
    "@canonry/world-schema": "workspace:*",
    "@the-canonry/shared-components": "workspace:*",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "seedrandom": "^3.0.5",
    "serve": "^14.2.5",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@module-federation/vite": "^1.3.1",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.1.1",
    "vite": "^7.2.4"
  }
}
